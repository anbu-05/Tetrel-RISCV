# ignore everything under sim/
sim/*

# ...except the run script
!sim/*.do

# (optional) keep the folder even when empty
!sim/.gitkeep

# ignore all hidden files (like .DS_Store, .vscode/, etc.)
.* 